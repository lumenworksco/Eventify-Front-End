// Thin wrapper to keep compatibility for JS import paths while delegating
// implementation to the typed `Layout.tsx` file. This avoids duplicate code
// while keeping older import semantics working.
import Layout from './Layout';

export default Layout;
